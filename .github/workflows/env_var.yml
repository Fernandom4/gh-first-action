name: Deployment
on:
  push:
    branches:
      - master
      - dev
env:
  MONGO_DB_NAME: content_db
jobs:
  test:
    env:
      MONGODB_USERNAME: fernandom4
      MONGODB_PASSWORD: fernando123*
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install Dependencies
        run: npm ci
      - name: Calling env variable method 1
        run: echo "MONGODB_USERNAME: $MONGODB_USERNAME" #Interpolated
      - name: Calling env variable method 2
        run: echo "MONGODB_USERNAME: ${{ env.MONGODB_USERNAME }}"

        
